{
  "name": "backend",
  "version": "1.0.0",
  "description": "Payment tracker API",
  "main": "index.js",
  "scripts": {
    "start": "sucrase-node server.js",
    "server": "nodemon --exec sucrase-node server.js",
    "client": "npm start --prefix ../",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.4.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.9.5",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "concurrently": "^5.1.0",
    "nodemon": "^2.0.2",
    "sucrase": "^3.12.1"
  }
}
